#include<iostream>
#include<algorithm>
using namespace std;
int main(void){
	int N, K;
	cin >> N >> K;
	int C[N];
	for(int i = 0; i < N; i++)
		cin >> C[i];
	sort(C,C+N);
	int result=0;
	int l=0;
	for(int i=0;i<N;i++){
		l=i/K;
		result+=(l+1)*C[i];
	}
	cout << result << "\n";

	return 0;
}

